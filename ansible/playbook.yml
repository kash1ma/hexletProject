---
- name: Deploy Laravel application to Yandex Cloud
  hosts: 51.250.111.163
  become: yes
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install dependencies
      apt:
        name: "{{ item }}"
        state: present
      loop:
        - curl
        - docker.io
        - docker-compose

    - name: Ensure Docker service is running
      service:
        name: docker
        state: started
        enabled: yes

    - name: Stop existing Docker container
      docker_container:
        name: user-crud-app
        state: stopped
        force_kill: yes
        ignore_errors: yes

    - name: Remove existing Docker container
      docker_container:
        name: user-crud-app
        state: absent
        ignore_errors: yes

    - name: Pull the latest Docker image
      docker_image:
        name: your_dockerhub_username/user-crud-app
        tag: latest
        source: pull

    - name: Run the Docker container
      docker_container:
        name: user-crud-app
        image: your_dockerhub_username/user-crud-app:latest
        state: started
        restart_policy: always
        ports:
          - "80:80"

  handlers:
    - name: Restart Docker
      service:
        name: docker
        state: restarted
